<script setup lang="ts">
interface Props {
    title: string
    icon?: string
}

const props = defineProps<Props>()
</script>

<template>
    <div class="grid gap-1">
        <div class="flex items-center gap-1 pl-2">
            <Icon v-if="props.icon" :name="props.icon" class="text-muted size-3.5" />
            <span class="text-muted text-[0.8rem]">{{ props.title }}</span>
        </div>

        <slot />
    </div>
</template>
